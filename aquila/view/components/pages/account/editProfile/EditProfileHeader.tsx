import classes from './EditProfileHeader.module.scss';

const EditProfileHeader = () => {
    return (
        <header className={classes.header}>
            <h3 className={classes.header__title}>Edit Profile</h3>
        </header>
    )
}

export default EditProfileHeader;